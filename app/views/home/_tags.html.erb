<ul class="post-search-results">
  <% @tags.each do |tag| %>
    <li data-id="<%= tag['id'] %>">
      <span class="post-info">
        <%= render :partial => 'post_info', :locals => { :data => tag } %>
      </span>
      <input type="button" value="Add" class="add-favorite">
    </li>
  <% end %>
</ul>